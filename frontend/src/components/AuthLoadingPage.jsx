import LogoSplash from './LogoSplash';

/** OAuth / session sync — logo shown only while auth finishes (usually brief) */
const AuthLoadingPage = () => <LogoSplash />;

export default AuthLoadingPage;
