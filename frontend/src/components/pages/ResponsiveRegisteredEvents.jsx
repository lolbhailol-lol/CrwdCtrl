import React from 'react';
import RegisteredFest from './profile-pages/registered-fest';

const ResponsiveRegisteredEvents = () => {
    // Use RegisteredFest for all screen sizes since it's already responsive
    return <RegisteredFest />;
};

export default ResponsiveRegisteredEvents;